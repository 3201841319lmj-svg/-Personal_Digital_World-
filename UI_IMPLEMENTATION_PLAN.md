# AI Home Farm Interface - 首页 UI 实现计划 V4.0 (UI_IMPLEMENTATION_PLAN.md)

## 1. 动态生活状态与极简 UI 计划

本阶段重点：将 `HomeShowcaseCard` 升级为展示“家园今天正在发生什么”的动态生活状态组件，精简高宽，完全参考用户提供的两张最新设计参考图。

---

## 2. 详细实现方案

### 2.1 家园档案组件 (`HomeShowcaseCard`)
1. **新增“餐厅”空间**：导航标签更新为 `🌱 农场` | `✨ 工坊` | `📖 书房` | `🍽️ 餐厅`（魔法工坊统一名称改为“工坊”）。
2. **餐厅时间段切图**：早餐 `06:00-12:00` / 午餐 `12:00-18:00` / 晚餐 `18:00-06:00`。
3. **取消复杂标题，改为“动态生活状态展示”**：
   - 布局：图片区域 ➔ 动态生活状态（例：`🌱 播种 🍅 番茄`） ➔ 资源信息（最多4项单行）。
4. **4 空间状态库随机生成器**：
   - 农场：播种/浇水/除草/收获/出售 + 番茄/黄瓜/苹果/小麦/南瓜等。
   - 工坊：加工/研究/制作/实验 + 矿石/魔法材料/果酱/家具/药剂等。
   - 书房：阅读/整理/记录/研究 + 书籍/笔记/论文/地图/古籍等。
   - 餐厅：制作/烹饪/摆放/整理 + 面包/沙拉/汤/甜点/果汁等。

### 2.2 全包容留言桌 (`FarmChatComposer`) 重构 (参考用户设计图 2)
- **小青植 🌿 移至输入框上方**：摆放在输入框卡片上方左侧。
- **输入框扩展为两行高度**：提升书写体验，Placeholder 为 `听候农场主的指示……`。
- **底部内嵌工具栏**：
  - 左侧：`📎` (附件) + `👥` (伙伴带数字徽章) + `✦` (思考深度)
  - 右侧：`✈️` 绿色纸飞机发送按钮（置于输入框卡片右下角）。

### 2.3 客厅 Header 重构 (参考用户设计图 1)
- **删除农场主签名行**：释放垂直空间。
- **单行按钮整合**：
  - `🔍` 搜索聊天记录
  - `📑` 话题目录 (用户历史话题跳转)
  - `✨` 新建话题
  - `⚙️` 唯一设置入口
- **保留 `📑` 话题目录**：点击展开用户历史提问，点击跳转对应消息。

---

## 3. 第四阶段代码修改步骤

1. **更新 3 份架构 Markdown 规范**（**已完成**）。
2. **扩充类型与状态库**：
   - 在 `src/types/index.ts` 中加入 `restaurant` 场景及 `ActionItemStatus` 接口。
   - 在 `src/data/livingStatusData.ts` 中创建 4 大空间的动作与物品数据库。
   - 更新 `src/data/defaultData.ts` 补充餐厅预设与 4 空间资源。
3. **改写 `HomeShowcaseCard.tsx`**：
   - 支持 4 Tab 选择。
   - 渲染图片 + 动态生活状态（`[动作图标][动作文字] [物品图标][物品文字]`） + 4 项资源。
4. **改写 `FarmChatComposer.tsx`**：
   - 将 🌿 移动到输入框上方左侧。
   - 调整卡片为两行高度，底部左侧放 `📎` `👥` `✦`，右侧放 `✈️`。
5. **改写 `LivingRoomPage.tsx`**：
   - 删除签名行，单行放 `🔍` `📑` `✨` `⚙️` 按钮。
   - 保持 `📑` 话题目录与 `🔍` 搜索聊天记录。
6. **运行 `npm run build`** 确保 100% 编译成功！
