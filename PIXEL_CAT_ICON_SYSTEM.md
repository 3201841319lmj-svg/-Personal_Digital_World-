# 个人数字世界像素猫图标系统

版本：1.0
建立日期：2026-09-02
状态：正式使用

## 1. 目标

个人数字世界下部署到手机、电脑桌面或 PWA 的网页统一使用“大颗粒像素猫”作为项目图标。所有图标一眼属于同一家族，同时通过职业、动作和道具区分项目功能。

## 2. 固定家族特征

以下项目间不得随意改变：

- 单只、正面或轻微三分之二视角的圆润猫猫。
- 大颗粒方形像素、硬边、清晰网格，不使用抗锯齿或照片质感。
- 小耳朵、短四肢、圆脸、腮红、可识别的卷尾。
- 主体约占画布 70%–74%，所有关键部位位于中央 72% mask-safe 区域。
- 满版浅色背景，加一层扁平的浅色圆角方形底板；圆角使用阶梯像素边。
- 无文字、字母、数字、水印、复杂场景和零散装饰。
- 缩小到 48×48 时仍能识别猫猫与核心道具。

## 3. 可变化部分

每个项目只从以下四个维度表达差异：

1. 职业服装：例如烘焙帽、园艺围裙、档案员背心。
2. 单一核心道具：例如面包、幼苗、文件夹、星球。
3. 单一动作：抱、举、浇水、记录、观察。
4. 一个项目强调色；白色猫身、奶油背景和深色轮廓体系保持稳定。

每枚图标只允许一个核心道具，避免在手机桌面形成视觉噪声。

## 4. 基础色彩

| 角色 | 色值 | 用途 |
|---|---|---|
| 奶油背景 | `#FFF4CF` | 图标满版背景 |
| 暖奶油底板 | `#FFE8A8` | 阶梯圆角方形底板 |
| 猫身 | `#FFFDF8` | 固定主体色 |
| 腮红 | `#F49AA0` | 面部小面积点缀 |
| 深轮廓 | `#244EB7` | 默认像素轮廓；项目可在同等对比度下替换 |
| 咖啡色 | `#795642` | 个人数字世界暖色基准 |

允许因生成与像素阴影出现少量邻近色，但不得使用连续渐变、发光、模糊或半透明边缘。

## 5. 母版与导出规格

每个项目保存一个 `1024×1024` PNG 母版（RGB、RGBA 或无抖动索引色），并使用最近邻缩放导出：

| 文件 | 尺寸 | 用途 |
|---|---:|---|
| `icon-master-1024.png` | 1024×1024 | 项目母版与归档 |
| `icon-512.png` | 512×512 | PWA 高分辨率图标 |
| `icon-maskable-512.png` | 512×512 | Android adaptive/maskable 图标 |
| `icon-192.png` | 192×192 | Android/PWA 常规图标 |
| `apple-touch-icon.png` | 180×180 | iOS/iPadOS 主屏幕 |
| `favicon-48.png` | 48×48 | 浏览器标签与兼容场景 |
| `favicon-32.png` | 32×32 | 浏览器标签 |
| `favicon-16.png` | 16×16 | 旧浏览器兼容 |
| `favicon.ico` | 16/32/48 | 浏览器 favicon |

禁止对小尺寸图标重新绘制或使用平滑插值；统一从批准的母版最近邻缩放。

## 6. 编号与登记

| 编号 | 项目 | 角色 | 道具 | 强调色 | 状态 |
|---|---|---|---|---|---|
| `CAT-001` | `today-todo-app` | Baker / 烘焙猫 | 金色面包 | 钴蓝 + 焦糖 | 已采用 |

新图标使用下一个连续编号，并在本表登记项目、角色、核心道具、强调色和状态。

## 7. 项目接入要求

- Web App Manifest 必须包含 `192×192`、`512×512` 和 `maskable 512×512`。
- Next.js 项目同时声明 favicon、Apple Touch Icon、应用名称和主题色。
- `start_url` 与 `scope` 必须对应项目实际域名或子路径。
- Android 圆形、圆角方形与 iOS 圆角裁切均不得切掉耳朵、帽子、尾巴或道具。
- 上线后在手机窄视口检查 Manifest、图标请求、桌面名称和 standalone 显示模式。

## 8. 新图标制作流程

1. 明确项目功能，只选择一个职业、一个动作、一个核心道具和一个强调色。
2. 以现有批准图标作为家族风格参考，不复制具体姿势或道具。
3. 先生成 1024×1024 母版，检查 72% mask-safe 区域。
4. 在 48×48 预览下确认轮廓与道具仍可辨认。
5. 用户批准后再导出全套尺寸并接入项目。
6. 执行类型检查、构建、Manifest 验证和公网资源检查后部署。
